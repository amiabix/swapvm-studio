# Submission images

Upload the PNG files, not the SVG sources.

- `logo.png`: 512 × 512. Existing Studio S mark, exported from `logo.svg`.
- `cover.png`: 1600 × 900 (16:9). Cover artwork from `cover.svg`, matching the app typography and colors.
- `01-sepolia-atomic-receipt.png`: Actual application screenshot of the successful Sepolia transaction, all six stages and the settlement summary.
- `02-token-transfers.png`: Actual application screenshot of the eight ERC20 transfers and receipt inspection controls.
- `03-configure-trade.png`: Actual application screenshot of the composer in local test mode. Both local mock tokens report symbol T; the tokens are distinct contracts.

Screenshots captured September 13, 2026. Sepolia status was read from live RPC during capture; confirmations and head height are snapshots. The composer screenshot does not claim a new public transaction. No wallet connection, balances, transaction status or other application content was fabricated for the screenshots.

Logo and cover are deterministic SVG exports, not generated application screenshots. Source SVGs are included for editing.
